{
  "private": true,
  "type": "module",
  "bin": {
    "kana": "./kana"
  },
  "scripts": {
    "bench:tui-render": "bun scripts/bench-tui-render.ts",
    "build:cli": "bun build --compile --outfile=kana src/main.ts",
    "install:bin": "./scripts/install.sh",
    "start": "bun run build:cli && ./kana",
    "tui": "bun run start",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.49",
    "commander": "^15.0.0",
    "shiki": "^4.2.0",
    "string-width": "^8.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
